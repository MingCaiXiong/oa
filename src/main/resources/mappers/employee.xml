<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.xiongmingcai.oa.dao.EmployeeDao">
    <!-- 查询全部单个用户 -->
    <select id="queryById" parameterType="Long" resultType="top.xiongmingcai.oa.entity.Employee">
        select *
        from adm_employee
        where employee_id = #{value}
    </select>
</mapper>